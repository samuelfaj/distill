# Modified for Distill by Samuel Fajreldines, 2026.
[package]
license = "Apache-2.0"
edition.workspace = true
name = "distill-agent-lifecycle"
version = "0.1.0"

[dependencies]
async-trait = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
tokio = { workspace = true, features = ["macros", "rt"] }

[lints]
workspace = true
