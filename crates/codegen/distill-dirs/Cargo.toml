# Modified for Distill by Samuel Fajreldines, 2026.
[package]
license = "Apache-2.0"
edition.workspace = true
name = "distill-dirs"
version = "0.1.0"
description = "Home-directory resolution generally: USERPROFILE-first home_dir, plus Distill profiles ($DISTILL_HOME or <home>/.distill), with legacy profile reuse"

[dependencies]
dunce = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
pretty_assertions = { workspace = true }
tempfile = { workspace = true }

[lints]
workspace = true
