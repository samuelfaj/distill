# Modified for Distill by Samuel Fajreldines, 2026.
[package]
name = "distill-image"
version = "0.1.0"
edition.workspace = true
license = "Apache-2.0"
description = "Image validation and transcoding shared by Grok tools and clients"

[dependencies]
crc32fast = { workspace = true }
image = { workspace = true, features = ["png", "jpeg", "gif", "webp", "bmp", "tiff", "ico"] }
thiserror = { workspace = true }

[dev-dependencies]
distill-test-utils = { workspace = true }

[lints]
workspace = true
