#!/usr/bin/env node
// On Unix npm installs, postinstall.js replaces this entry with a symlink to
// the sibling distill-native binary; every other install launches through node.
require('./distill-bootstrap.js');
