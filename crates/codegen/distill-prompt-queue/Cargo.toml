# Modified for Distill by Samuel Fajreldines, 2026.
[package]
edition.workspace = true
license = "Apache-2.0"
name = "distill-prompt-queue"
version = "0.1.0"
description = "Shared prompt-queue wire types for distill-shell and distill-pager"

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }

[lints]
workspace = true
